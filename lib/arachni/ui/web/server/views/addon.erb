
            <div id="page-intro">
                <h2><%=escape( addon['name'] )%> v<%=escape( addon['version'] )%></h2>
                <p>
                    <%=escape( addon['description'] )%>
                    <br/>
                    <em>By <%=escape( addon['author'] )%></em>
                </p>
            </div>

            <%= erb :flash, {:layout => false} %>


            <%= erb tpl, {:layout => false}, tpl_args %>

